import{BaseCocoonShim as r}from"./_baseShim.js";import i from"./fs-shim.js";import t from"./process-shim.js";class l extends r{_serviceBrand;constructor(e,s){super("HostUtils",e,s),this._logInfo("Initialized.")}_requiresRpc(){return!1}get pid(){try{return t.pid}catch(e){this._logError("Error accessing 'pid' from processShimInstance. This may indicate an issue with process-shim.ts initialization or availability.","Error details:",e);return}}exit(e){this._logWarn(`IHostUtils.exit(${e}) called. Delegating to global 'process.exit()', which is subject to Cocoon's host termination policy (controlled via allowExitFn in cocoon-bootstrap.ts).`),process.exit(e)}async fsExists(e){if(this._logService?.trace(`fsExists check initiated for path: '${e}' (will use fs-shim).`),this._logWarnOnce("fsExists (and fsRealpath) rely on fs-shim.ts, whose Mountain backend (native_fs.rs) is DEPRECATED. These file operations are likely non-functional. Extensions should use vscode.workspace.fs."),!i?.promises?.stat)return this._logError("fs-shim.promises.stat is unavailable for fsExists check. This is likely due to fs-shim.ts issues or its deprecated backend in Mountain. Cannot perform existence check. Returning false."),!1;try{return await i.promises.stat(e),!0}catch(s){return s.code==="ENOENT"||s.code==="ENOTDIR"||this._logWarn(`fsExists check for path "${e}" encountered an unexpected error during 'stat' operation. This could be due to permissions, the path existing but being inaccessible, or the fs-shim backend failing. Error: ${s.message}. Returning false as per fsExists contract on error.`),!1}}async fsRealpath(e){if(this._logService?.trace(`fsRealpath request initiated for path: '${e}' (will use fs-shim).`),this._logWarnOnce("fsRealpath (and fsExists) rely on fs-shim.ts, whose Mountain backend (native_fs.rs) is DEPRECATED. These file operations are likely non-functional. Extensions should use vscode.workspace.fs."),!i?.promises?.realpath){const s="fs-shim.promises.realpath is unavailable. This might be due to fs-shim.ts issues or its deprecated backend in Mountain. Cannot perform realpath operation.";throw this._logError(s),new Error(s)}try{return await i.promises.realpath(e)}catch(s){throw this._logWarn(`fsRealpath for path "${e}" failed. This is expected if the path does not exist, permissions are insufficient, or the fs-shim backend is non-functional. Error: ${s.message}`),s}}dispose(){super.dispose(),this._logInfo("Disposed.")}}export{l as ShimHostUtils};
